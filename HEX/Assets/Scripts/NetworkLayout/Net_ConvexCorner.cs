using UnityEngine;
using System.Collections;
using Assets.NetworkLayout;

public class Net_ConvexCorner : INet_Block 
{
    //private int health = 100;
    //protected PersistentData manager;
	
    //private void Start()
    //{
    //    manager = GameObject.Find ("Persistence").GetComponent<PersistentData> ();
    //}
	
    //private void Update()
    //{
    //    if(photonView.isMine)
    //    {
    //        if(manager.Blocks[this.photonView.viewID] != 0)
    //        {
    //            health -= manager.Blocks[this.photonView.viewID];
    //            manager.Blocks[this.photonView.viewID] = 0;
    //        }
    //    }
    //    if(health <= 0)
    //        PhotonNetwork.Destroy(this.gameObject);
    //}
}
